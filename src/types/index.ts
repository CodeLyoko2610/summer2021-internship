export type restaurant = {
  blurhash: string,
  location: number[],
  name: string,
  launch_date: Date,
  online: boolean,
  popularity: number
}

export type coords = { longitude: number, latitude: number }